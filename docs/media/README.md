# Demo media

These SVGs are lightweight stand-ins for real screenshots/GIFs while the
project is still being shaped locally.

Planned replacements:

- `dashboard-overview.svg` → actual dashboard screenshot
- `project-detail.svg` → actual single-project screenshot
- `training-run.gif` → short run lifecycle demo (start → logs → metrics)

Keep filenames stable so README/docs links do not need churn when real
captures replace the placeholders.
